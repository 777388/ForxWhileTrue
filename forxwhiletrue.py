import sys
import os
class forxwhileTrue:
    def __init__(self, x=0, boolean=True):
        self.x = x
        self.boolean = boolean
    def run(self):
        while self.boolean:
            os.popen(**self.kwargs)
            self.x += 1
            continue
class forx(forxwhileTrue):
    def __init__(self):
        super(forx, self).__init__()

def forxx(self, **kwargs):
    self.kwargs = kwargs
    {lambda: forx
        .type(forxwhileTrue)
        .value(**self.kwargs)
        .trigger(False)
    }

forxx.run(sys.argv[1:])
